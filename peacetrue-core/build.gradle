description 'the core module, provide basic data structure and util class'

dependencies {
    api "com.google.code.findbugs:jsr305"//@Nullable注解
    implementation "javax.xml.bind:jaxb-api"
}

test {
    useJUnitPlatform()
}
