description 'the core module, provide basic data structure and util class'

dependencies {
    compile "com.google.code.findbugs:jsr305"//@Nullable注解
    compile "javax.xml.bind:jaxb-api"
    compileOnly 'junit:junit'//扩展断言方法
}
